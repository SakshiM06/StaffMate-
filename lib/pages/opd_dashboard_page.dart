import 'package:flutter/material.dart';

class OpdDashboardPage extends StatelessWidget {
  const OpdDashboardPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("OPD Dashboard (Coming Soon)"),
    );
  }
}
